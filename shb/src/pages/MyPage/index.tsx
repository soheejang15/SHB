import * as React from "react";

import Button from "../../components/Common/Input";

const MyPage: React.FC = () => {
  return (
    <Button width={300} height={50} fontSize={14} isAct={true}>
      <img src="" alt="" />
    </Button>
  );
};

export default MyPage;
